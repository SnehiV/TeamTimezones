{
  "database": {
    "rules": "database.rules.json"
  },
  "hosting": {
    "firebase": "team-timezone",
    "public": "dist",
    "rewrites": [
      {
        "source": "**",
        "destination": "/index.html"
      }
    ]
  }
}
